from math import sqrt, pow, factorial

num = 16
sqrt_res = sqrt(num)
print(f"The sqaure root of {num} is {sqrt_res}")

factorial_res = factorial(5)
print(f"The factorial of 5 is {factorial_res}")

base = 2
exp = 3
pow_res = pow(base, exp)
print(f"{base} raised to the power of {exp} is {pow_res}")
